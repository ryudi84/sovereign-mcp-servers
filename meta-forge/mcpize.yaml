version: 1
name: meta-forge-mcp
description: 'MCP server for generating HTML meta tags (Open Graph, Twitter Cards) and running SEO checks.'
runtime: typescript
entry: dist/index.js
build:
  install: npm install
  command: npm run build
startCommand:
  type: stdio
  command: node
  args:
    - dist/index.js
