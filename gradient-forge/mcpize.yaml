version: 1
name: gradient-forge-mcp
description: 'MCP server for CSS gradient generation — linear, radial, conic gradients with color stops and preview.'
runtime: typescript
entry: dist/index.js
build:
  install: npm install
  command: npm run build
startCommand:
  type: stdio
  command: node
  args:
    - dist/index.js
