startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |
    (config) => ({ command: 'npx', args: ['-y', 'sovereign-code-quality-mcp'] })
  exampleConfig: {}
