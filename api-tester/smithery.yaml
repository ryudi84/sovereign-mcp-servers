startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |
    (config) => ({ command: 'npx', args: ['-y', 'sovereign-api-tester-mcp'] })
  exampleConfig: {}
